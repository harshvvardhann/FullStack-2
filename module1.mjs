const a="Harsh";
const b="vardhan";
const c="singh";

// now we can by default export something like and we can give any name for importing it in other component as it will be our default export.
export default c;

//we can export other's also but for that we need to export it in some different way and also for import in other module we have to give same name which we have given here.
export {a}
export {b}